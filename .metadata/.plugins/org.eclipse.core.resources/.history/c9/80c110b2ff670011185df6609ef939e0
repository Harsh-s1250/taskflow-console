package com.taskflow.main;

import java.sql.Connection;
import java.time.LocalDate;

import com.taskflow.dao.UserDAO;
import com.taskflow.dao.impl.UserDAOImpl;
import com.taskflow.model.Priority;
import com.taskflow.model.Project;
import com.taskflow.model.Status;
import com.taskflow.model.Task;
import com.taskflow.model.User;
import com.taskflow.util.DBConnection;

public class Main {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		Project project = new Project(1, "TaskFlow", "Console App", 5);
		System.out.println(project);
		
		Task task = new Task(1, "Design UI", "Create Menu", Status.TODO, Priority.HIGH, LocalDate.of(2026, 6, 20), 5, 2);
		System.out.println(task);
		
		Connection con = DBConnection.getConnection();
		if(con!=null) {
			System.out.println("Success");
		}else {
			System.out.println("Fail");
		}
		
		UserDAO userDAO = new UserDAOImpl();
		User user = new User(1, "Harshad", "harshad@gmail.com","12334");
		
		boolean success = userDAO.register(user);
		if(success) {
			System.out.println("success");
		}else {
			System.out.println("fail");
		}
		
		User currUser = userDAO.login("harshad@gmail.com", "123456");
		
		if(currUser!=null) {
			System.out.println("Welcome "+currUser.getUsername());
		}else {
			System.out.println("Invalid credentials");
		}
	}

}
